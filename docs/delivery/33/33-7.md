# 33-7 Testes de consistência de cálculo e integração

[Voltar para lista de tarefas](./tasks.md)

## Description
Validar o contrato de integração entre metas (PBI 33) e otimização (PBI 32), cobrindo os cenários de referência manual de entrega e fallback histórico.

## Status History
| Data/Hora | Evento | De | Para | Detalhes |
|-----------|--------|----|------|----------|
| 2026-04-16 | Status Change | - | Proposed | Task criada no PBI 33 |
| 2026-04-16 | Status Change | Proposed | InProgress | Testes de integração para `optimization-context` iniciados |
| 2026-04-16 | Status Change | InProgress | Done | Testes automatizados criados e executados com sucesso |

## Requirements
- [x] Criar testes para endpoint de contexto de otimização.
- [x] Validar priorização de entrega manual.
- [x] Validar fallback para histórico quando manual indisponível.
- [x] Executar suíte de teste e registrar resultado.

## Implemented Tests
- Arquivo:
  - `test/integration/api/goals/optimization-context.test.ts`
- Casos cobertos:
  1. `volume_captured` disponível -> `delivered_source=manual_client`.
  2. `volume_captured` indisponível -> `delivered_source=historical_fallback`.

## Execution Evidence
- Comando:
  - `npm test -- test/integration/api/goals/optimization-context.test.ts`
- Resultado:
  - 2 testes executados.
  - 2 testes aprovados.

## Files Modified
- `test/integration/api/goals/optimization-context.test.ts`
- `app/api/goals/optimization-context/route.ts`
- `docs/delivery/33/tasks.md`
- `docs/delivery/33/33-7.md`

## Verification
- [x] Cenários críticos de integração cobertos.
- [x] Contrato do endpoint validado em teste automatizado.
- [x] Sem erros de lint nos arquivos alterados.

## Test Plan
Objetivo:
- Assegurar robustez do contexto de otimização antes do consumo efetivo pelo PBI 32.

Cenários:
- entrega manual disponível;
- fallback histórico aplicado;
- payload retornado com `success=true` e itens consistentes.

Critério de aprovação:
- Teste dedicado do endpoint com status verde na execução local.
