# 27-1 Criar infraestrutura de dados para ML

[Voltar para lista de tarefas](./tasks.md)

## Description
Fechar o **embamento** (decisão + desenho) e **migração** no Supabase alinhados ao [PBI 27](./prd.md), para que a **27-2** (modelo base) consuma uma série temporal diária por **adset** sem ambiguidade de contrato.

---

## O que você respondeu (produto) — resumo

| Tema | Sua resposta |
|------|----------------|
| Entrega da 27-1 | Decisão + desenho + migração (sem obrigar job novo nesta task, se o desenho reutilizar a carga existente). |
| Primeiro consumidor | Em teoria a **27-2** (modelo base); não exige tela nesta task. |
| Plataforma | Manter **Supabase (Postgres)**; stack atual (GitHub, Vercel, etc.). |
| Operação | Você; padrão de jobs no repo será verificado na implementação. |
| Entidade e grão | **Adset**; **uma linha por dia** (`date`). |
| Histórico | **Nice to have ~1 ano** de histórico para treino/análise. |
| Fonte de dados | **Em teoria só `adset_insights`**. |
| Fuso | Regra de negócio em **America/Sao_Paulo** (dias de calendário como no restante do produto). |
| CPL / métricas | **Mesma regra** que o dashboard hoje. |
| Nomes (campanha/adset) | Confirmado o desenho: **sem duplicar**; expor o que já existe em `adset_insights` / joins. |
| 27-2 / SLA | Ainda sem definição de métricas-alvo nem número alvo de performance. |
| DoD formal | Ver seção **Definition of Done** abaixo (fechado). |

---

## Onde faltava detalhe — recomendações técnicas (registro)

### 1) “TimescaleDB” vs o que faremos no Supabase
- **Situação:** O repositório já tem **`adset_insights`** com chave **`UNIQUE (adset_id, date)`** e índice `(adset_id, date)` — ou seja, **já é uma série temporal diária** adequada como base do MVP.
- **Decisão:** **Não** introduzir serviço novo (Timescale Cloud) nem depender de extensão Timescale no Postgres até haver necessidade comprovada.
- **Execução:** View estável `public.v_ml_adset_daily_series` sobre `adset_insights` (migração `20260420140000_27_1_ml_adset_daily_series_view.sql`).

### 2) Dias sem dados
- **Decisão:** Manter **esparso** no banco (sem linha = sem dado naquele dia).
- **27-2:** imputar zeros só na preparação do dataset, se o modelo exigir série contínua.

### 3) Backfill / idempotência
- **Base:** `UNIQUE (adset_id, date)` em `adset_insights`; cargas devem usar **UPSERT** por esse par natural.
- **View:** somente leitura; idempotência de dados continua responsabilidade do pipeline que grava em `adset_insights`.

### 4) RLS / `security_invoker`
- **View:** criada com **`WITH (security_invoker = true)`** (PostgreSQL 15+): avalia permissões e RLS **como o usuário que consulta**, não como dono da view.
- **Consumo previsto:** APIs Next com **`service_role`** (bypass RLS). `GRANT SELECT` na view foi concedido apenas a **`service_role`** para não ampliar superfície a `anon`.

---

## Decisões fechadas (confirmado **OK nos 4** em 2026-04-20)

1. **MVP = `adset_insights` canônico**, sem novo banco Timescale.
2. **Série esparsa**; zeros na preparação do modelo na **27-2**.
3. **Nomes:** sem tabela nova de nomes; view reutiliza colunas existentes (`adset_name`, etc.).
4. **Migração:** view + comentários + smoke documentado abaixo.

---

## Contrato da view `public.v_ml_adset_daily_series`

| Coluna na view | Origem | Notas |
|----------------|--------|--------|
| `adset_id` | `adset_insights.adset_id` | Chave de negócio do adset. |
| `metric_date` | `adset_insights.date` | Dia civil; par único com `adset_id` na origem. |
| `spend` | `adset_insights.spend` | Mesma unidade/semântica da tabela base (ver comentários históricos das migrações). |
| `impressions` | `adset_insights.impressions` | Inteiro. |
| `clicks` | `adset_insights.clicks` | Inteiro. |
| `leads` | `adset_insights.leads` | Inteiro; alinhado ao dashboard. |
| `cpl` | `adset_insights.cpl` | Quando preenchido na origem; mesma regra do produto. |
| `ctr`, `cpc`, `cpm` | idem | Derivados/agregados na origem. |
| `reach`, `frequency` | idem | Opcionais para features futuras. |
| `adset_name` | `adset_insights.adset_name` | Sem cópia em outra tabela. |
| `campaign_id`, `account_id`, `status` | idem | Contexto opcional para modelo ou filtros. |
| `created_at`, `updated_at` | idem | Auditoria / frescor do dado. |

---

## Queries de smoke (rodar após `supabase db push` / aplicar migração)

Substitua `'<SUBSTITUIR_ADSET_ID>'` por um `adset_id` real existente em `adset_insights` (mantenha as aspas simples se o ID for texto).

**1) Janela ~90 dias — contagem e min/max de datas**

```sql
SELECT
  count(*) AS rows_90d,
  min(metric_date) AS d0,
  max(metric_date) AS d1
FROM public.v_ml_adset_daily_series
WHERE adset_id = '<SUBSTITUIR_ADSET_ID>'
  AND metric_date >= (current_date - interval '90 days')::date
  AND metric_date <= current_date;
```

**2) Janela ~365 dias (nice to have)** — mesmo padrão com `interval '365 days'`.

**3) Esparsidade** — dias no calendário vs linhas (esperado: calendário ≥ linhas)

```sql
SELECT
  (current_date - (current_date - interval '90 days')::date) AS calendar_days_approx,
  count(*) AS row_days
FROM public.v_ml_adset_daily_series
WHERE adset_id = '<SUBSTITUIR_ADSET_ID>'
  AND metric_date >= (current_date - interval '90 days')::date;
```

---

## Requirements (checklist)

- [x] Decisão de plataforma: **Supabase Postgres**, sem Timescale externo na 27-1.
- [x] Contrato documentado da série diária por **adset** (colunas e chave `(adset_id, metric_date)`).
- [x] Migração com **view** + **COMMENT** para a 27-2.
- [x] Avaliação **RLS / security_invoker** documentada acima.
- [x] Query de **smoke** documentada (90d + 365d).
- [x] Padrão de **idempotência** documentado (`UNIQUE` + UPSERT na origem).

## Implementation Plan (executado)

1. ~~Confirmar contrato de `adset_insights` no repo~~ — feito.
2. ~~Documentar mapeamento de colunas~~ — tabela acima.
3. ~~Migração: view + comentários~~ — `supabase/migrations/20260420140000_27_1_ml_adset_daily_series_view.sql`.
4. ~~Smoke SQL~~ — seção acima + `scripts/27-1-smoke.sql` e `scripts/27-1-apply-and-smoke.sh`.
5. ~~Ordem operacional~~ — seção **Ordem de execução (acompanhamento)**; smoke e Done ficam com você após `link` + `db push` ou script com `DATABASE_URL`.

## Ordem de execução (acompanhamento)

Siga na ordem. O que já foi feito neste repositório está marcado.

| Passo | O quê | Quem / Onde |
|-------|--------|-------------|
| **1** | Garantir que o Supabase CLI roda **na pasta do repositório** (há `supabase/config.toml` para ancorar o projeto; evita o CLI subir para a home). | Você / CI |
| **2a** | **`supabase link`** (uma vez por máquina), se ainda não existir link para o projeto certo. | Você |
| **2b** | **`supabase db push`** para aplicar migrações pendentes (inclui `20260420140000_27_1_ml_adset_daily_series_view.sql`). | Você |
| **2c** | *Alternativa ao 2b:* exportar `DATABASE_URL` (string Postgres do painel Supabase) e rodar **`./scripts/27-1-apply-and-smoke.sh`** (aplica o mesmo arquivo SQL + smoke). | Você |
| **3** | Rodar smoke: o script **`scripts/27-1-smoke.sql`** (chamado pelo passo 2c) ou copiar as queries da seção acima no SQL Editor do Supabase. | Você |
| **4** | Se `sample_adset_id` não aparecer: não há linhas em `v_ml_adset_daily_series` nos últimos 90 dias — valide sync/`adset_insights` antes de concluir. | Você |
| **5** | Marcar os checkboxes em **Verification** abaixo e mover a task **27-1** para **Done** em `tasks.md` quando estiver satisfeito. | Você |

**Nota (execução neste ambiente de agente):** `supabase db push` retornou `Cannot find project ref. Have you run supabase link?` — esperado sem credenciais de projeto no CI; no seu Mac, após o `link`, o passo 2b deve aplicar a view.

### Problema: `Remote migration versions not found in local migrations directory`

**Causa:** o remoto tem na tabela `supabase_migrations.schema_migrations` uma versão que **já não existe** como ficheiro local (ex.: `20240516`), por exemplo depois de um `db push` parcial ou de **renomear** migrações para corrigir colisão de `version`.

**O que fazer (na raiz do repo, projeto linkado):**

1. Marcar essa versão órfã como revertida (o CLI sugere o mesmo padrão):

```bash
supabase migration repair --status reverted 20240516
```

2. Voltar a aplicar migrações:

```bash
supabase db push
```

**Opcional — ver diferenças local vs remoto:**

```bash
supabase migration list --linked
```

Se aparecerem **outras** versões no remoto sem ficheiro local correspondente, repita o `repair --status reverted <versão>` para cada uma (só após confirmar que é seguro; em dúvida, copie o output do `migration list` para revisão).

## Verification

- [ ] Smoke (90d) executado em ambiente com migração aplicada; totais coerentes com amostra no dashboard.
- [ ] Smoke opcional 365d se houver dados suficientes.
- [x] Migração revisada: `security_invoker` + `GRANT` apenas `service_role`.

## Files Modified

- `docs/delivery/27/27-1.md`
- `supabase/migrations/20260420140000_27_1_ml_adset_daily_series_view.sql`
- `supabase/config.toml` (ancoragem do Supabase CLI ao repositório)
- `scripts/27-1-smoke.sql`
- `scripts/27-1-apply-and-smoke.sh`

## Test Plan

- **Objective:** View estável da série `(adset_id, metric_date)` para a 27-2.
- **Scope:** View/comentários/smoke; sem treinar modelo.
- **Env/Setup:** Supabase após migração.
- **Scenarios:** (1) série 90d; (2) série esparsa; (3) opcional 365d.
- **Criteria:** Requirements [x]; smoke humano executado.

---

## Status History

| Data/Hora | Evento | De | Para | Detalhes |
|-----------|--------|----|------|----------|
| 2026-04-20 | Status Change | Proposed | InProgress | Início da task; backlog e PBI 27 alinhados a execução |
| 2026-04-20 | Nota | — | — | Respostas do produto incorporadas; decisões técnicas onde havia lacunas |
| 2026-04-20 | Confirmação produto | — | — | OK nos 4 itens (canônico adset_insights, série esparsa, nomes sem duplicar, migração view+comments+smoke) |
| 2026-04-20 | Implementação | InProgress | Review | Migração `v_ml_adset_daily_series` + documentação de smoke e contrato |
| 2026-04-20 | Operação | — | — | Adicionados `supabase/config.toml`, scripts de smoke/apply, `.gitignore` para `.supabase/`; `supabase db push` no agente exige `supabase link` no seu ambiente |
