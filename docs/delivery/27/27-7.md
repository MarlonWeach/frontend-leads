# 27-7 Criar interface visual de previsões

[Voltar para lista de tarefas](./tasks.md)

## Description
Construir a primeira interface visual para exibir previsões, intervalos de confiança e comparação de cenários gerados pelas tasks anteriores.

## Requirements
- [x] Exibir previsões por horizonte com visualização clara.
- [x] Exibir intervalo de confiança e cenário selecionado.
- [x] Suportar filtros principais (conta/campanha/adset/período).
- [x] Garantir UX consistente com padrões do projeto.

## Implementation Plan
1. Definir contrato de dados para frontend.
2. Implementar componentes de gráfico e cards de resumo.
3. Conectar filtros e estados de carregamento/erro.
4. Validar legibilidade e consistência visual.

## Verification
- [x] Interface renderiza previsões sem erro.
- [x] Filtros alteram dados exibidos corretamente.
- [x] Cenários e intervalos de confiança aparecem de forma consistente.

## Files Modified
- `docs/delivery/27/27-7.md`
- `src/components/insights/PerformanceForecast.tsx`
- `src/hooks/usePerformanceForecast.ts`
- `src/types/forecast.ts`

## Test Plan
- **Objective:** validar visualização útil e confiável das previsões.
- **Scope:** UI de previsões e interação de filtros.
- **Env/Setup:** endpoint/serviço de previsão disponível.
- **Scenarios:** troca de filtros, horizontes e cenários.
- **Criteria:** experiência estável e dados coerentes com backend.

## Premissas e limitações (fase atual)

- UI passou a mostrar cenários por métrica (`conservative`, `realistic`, `optimistic`) nos cards.
- Painel de qualidade por métrica foi ajustado para exibir Erro Percentual Absoluto Medio e status usando `metadata.accuracy`.
- Filtros de `campaignId` e `adsetId` foram adicionados na UI e conectados ao backend.

## Status History
| Data/Hora | Evento | De | Para | Detalhes |
|-----------|--------|----|------|----------|
| 2026-04-22 | Criação da task | — | Proposed | Documento inicial criado para execução futura |
| 2026-04-22 | Status Change | Proposed | InProgress | Início da implementação contínua após conclusão da 27-6 |
| 2026-04-22 | Implementação parcial | InProgress | InProgress | Cards exibem cenários e painel de accuracy; pendente completar filtros principais de segmentação |
| 2026-04-22 | Conclusão técnica | InProgress | Done | Filtros de campanha/adset conectados ao hook/API e UI validada com cenários + accuracy + alertas preditivos |
