# 28-27 Corrigir divergência Vercel em performance e warning zustand

[Voltar para lista de tarefas](./tasks.md)

## Description
Corrigir divergência de métricas entre local e Vercel na API `/api/performance` e remover aviso deprecado relacionado a `zustand` no bundle instrumentado em produção.

## Status History
| Data/Hora | Evento | De | Para | Detalhes |
|-----------|--------|----|------|----------|
| 2026-04-15 | Status Change | - | InProgress | Task iniciada para corrigir regressão de dados em produção e aviso de dependência |

## Requirements
- [x] Produção e local devem retornar os mesmos totais para período idêntico em `/api/performance`.
- [x] Eliminar fallback para fonte secundária que cause divergência silenciosa em produção.
- [ ] Remover aviso `[DEPRECATED] Default export is deprecated. Instead use import { create } from 'zustand'`.
- [ ] Validar resultado em Vercel após deploy.

## Implementation Plan
- Revisar `app/api/performance/route.ts` e remover fallback para Meta em erro de conexão com Supabase.
- Garantir uso de `supabaseServer` como fonte oficial e explicitar origem no payload.
- Atualizar dependência vinculada ao bundle instrumentado para versão sem aviso de `zustand`.
- Validar localmente com as mesmas chamadas de API usadas no diagnóstico.

## Verification
- [x] `GET /api/performance` local com período de ontem retorna totais corretos (392 leads / R$ 3903.61).
- [x] Payload de `/api/performance` inclui `source: "supabase"`.
- [ ] Aviso deprecado de `zustand` não aparece após deploy no Vercel.
- [ ] Ambiente Vercel retorna valores corretos no resumo de `/dashboard` e `/performance`.

## Files Modified
- `docs/delivery/28/tasks.md`
- `docs/delivery/28/28-27.md`
- `app/api/performance/route.ts`

## Test Plan
- Executar `npm run build`.
- Validar `curl` em `/api/performance` local para período de ontem.
- Após deploy, repetir validação no endpoint de produção e conferir UI em `/dashboard` e `/performance`.
