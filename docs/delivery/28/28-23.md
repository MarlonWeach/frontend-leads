# 28-23 Tratar 429 em otimização de IA com resiliência e UX

[Voltar para lista de tarefas](./tasks.md)

## Description
Corrigir o fluxo de otimização no painel de IA para lidar corretamente com `429 Too Many Requests` em `/api/ai/optimization`, evitando quebra de experiência e entregando feedback útil ao usuário.

## Status History
| Data/Hora | Evento | De | Para | Detalhes |
|-----------|--------|----|------|----------|
| 2026-04-14 | Status Change | - | Proposed | Task criada para resolver erro reportado de rate limit na otimização |
| 2026-04-14 | Status Change | Proposed | InProgress | Implementação iniciada para retornar fallback resiliente sem quebrar UI em 429 |
| 2026-04-15 | Status Change | InProgress | Done | Tratamento de 429 implementado com UX resiliente e fallback controlado |

## Requirements
- [x] Implementar tratamento explícito de 429 na API e no frontend.
- [x] Exibir mensagem orientativa ao usuário (limite atingido, quando tentar novamente).
- [x] Evitar loop de requisições em falha de quota/rate-limit.
- [x] Manter operação degradada segura (fallback ou estado estável sem quebrar tela).

## Implementation Plan
- Revisar `app/api/ai/optimization/route.ts` e mapear erro atual.
- Propagar código/metadata de rate limit para o cliente.
- Ajustar componente de otimização para UX de erro e retry controlado.
- Opcional: adicionar backoff simples e/ou cooldown local por sessão.

## Verification
- [x] Requisição com 429 não quebra UI do `/performance`.
- [x] Mensagem de erro amigável aparece no card de otimização.
- [x] Novo clique respeita janela de retry e não dispara spam de chamadas.

## Files Modified
- `docs/delivery/28/tasks.md`
- `docs/delivery/28/28-23.md`
- `app/api/ai/optimization/route.ts`
- `src/components/ai/OptimizationSuggestions.tsx`
- `src/components/ai/AIPanel.tsx`

## Test Plan
- Simular 429 localmente (mock/intercept) e validar comportamento da UI.
- Rodar cenário manual em produção após deploy.
- Cobrir fluxo com teste E2E determinístico para estado de rate limit.
