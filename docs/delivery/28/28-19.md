# 28-19 Estabilizar infraestrutura do Playwright no CI

[Voltar para lista de tarefas](./tasks.md)

## Description
Padronizar a execução do Playwright para reduzir flakiness estrutural e divergência entre ambiente local e CI, garantindo baseline confiável para análise dos cenários realmente funcionais.

## Status History
| Data/Hora | Evento | De | Para | Detalhes |
|-----------|--------|----|------|----------|
| 2026-04-14 | Status Change | - | Proposed | Task criada para estabilizar pipeline Playwright antes de correções de cenário |
| 2026-04-14 | Status Change | Proposed | InProgress | Execução iniciada com foco em CI determinístico (build/start) e artefatos |
| 2026-04-15 | Status Change | InProgress | Done | Infraestrutura CI do Playwright estabilizada e validada com build/start e artefatos |

## Requirements
- [x] Executar E2E em modo próximo de produção (`npm run build` + `npm run start`) quando em CI.
- [x] Garantir `baseURL` e variáveis mínimas coerentes entre workflow e `playwright.config.ts`.
- [x] Publicar artefatos de falha (report, trace, screenshot) em qualquer erro.
- [x] Documentar comando recomendado para debug local e no CI.

## Implementation Plan
- Revisar `.github/workflows/playwright.yml` para separar setup, build e execução.
- Ajustar `playwright.config.ts` para comportamento previsível em CI.
- Confirmar upload de `playwright-report` e `test-results`.
- Validar execução completa em branch com logs rastreáveis.

## Verification
- [x] Build de produção local (`npm run build`) concluído sem erro.
- [x] Workflow Playwright conclui setup sem erro de infraestrutura.
- [x] Relatório e traces são gerados automaticamente em caso de falha.
- [x] Taxa de falha flake reduzida em duas execuções sequenciais.

## Files Modified
- `docs/delivery/28/tasks.md`
- `docs/delivery/28/28-19.md`
- `.github/workflows/playwright.yml`
- `playwright.config.ts`

## Test Plan
- Disparar `workflow_dispatch` no GitHub Actions.
- Rodar localmente com configuração de CI para reproduzir.
- Comparar estabilidade entre duas execuções consecutivas.
