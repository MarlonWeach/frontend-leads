# 28-22 Corrigir fallback indevido em Variações e Performance (IA)

[Voltar para lista de tarefas](./tasks.md)

## Description
Investigar por que as análises "Variações" e "Performance" no painel de IA em `/performance` estão entrando em fallback, e corrigir a causa raiz para restabelecer resposta primária adequada.

## Status History
| Data/Hora | Evento | De | Para | Detalhes |
|-----------|--------|----|------|----------|
| 2026-04-14 | Status Change | - | Proposed | Task criada a partir de erro funcional reportado pelo usuário em produção |
| 2026-04-14 | Status Change | Proposed | InProgress | Investigação confirmou fallback indevido em `variations`; correção iniciada no backend de análise |
| 2026-04-15 | Status Change | InProgress | Done | Fluxo de Variações/Performance ajustado para reduzir fallback indevido e melhorar consistência |

## Requirements
- [x] Identificar origem do fallback (prompt, modelo, quota, timeout, payload, parsing, etc.).
- [x] Registrar logs diagnósticos suficientes para rastrear falha em produção.
- [x] Corrigir fluxo para reduzir fallback indevido mantendo segurança.
- [x] Garantir mensagem de UI coerente quando fallback legítimo ocorrer.

## Implementation Plan
- Mapear fluxo completo: UI -> hook -> API route -> provider IA.
- Reproduzir em ambiente local e Vercel com payload controlado.
- Corrigir o ponto de falha e ajustar observabilidade.
- Validar resposta final de "Variações" e "Performance" sem fallback indevido.

## Verification
- [x] Botões "Variações" e "Performance" geram resposta principal em produção.
- [x] Logs indicam caminho primário concluído sem erro.
- [x] Fallback só é acionado em condição real de contingência.

## Files Modified
- `docs/delivery/28/tasks.md`
- `docs/delivery/28/28-22.md`
- `src/hooks/useAIAnalysis.ts`
- `app/api/ai/analyze/route.ts`
- `src/components/ai/AIPanel.tsx`

## Test Plan
- Testar manualmente `/performance` com os dois botões de análise.
- Validar retorno via DevTools (status, payload, latência).
- Executar testes unitários/integrados relacionados ao fluxo de IA.
