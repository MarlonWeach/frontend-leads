# 28-18 Automatizar reconciliação diária do heatmap vs SQL

[Voltar para lista de tarefas](./tasks.md)

## Description
Implementar uma auditoria automatizada que compare os totais diários retornados pela API `/api/performance/comparisons` com os totais agregados diretamente de `adset_insights`, para detectar regressões de mapeamento/cache/RLS antes de impactar o heatmap.

## Status History
| Data/Hora | Evento | De | Para | Detalhes |
|-----------|--------|----|------|----------|
| 2026-04-14 | Status Change | Proposed | InProgress | Task iniciada para criar auditoria diária de paridade da API de comparisons |
| 2026-04-14 | Status Change | InProgress | Done | Workflow CI agendado e execução local validada com paridade OK |

## Requirements
- [x] Comparar diariamente `spend`, `leads`, `impressions`, `clicks` entre API e SQL.
- [x] Cobrir janela configurável (default 30 dias).
- [x] Retornar erro (exit code 1) quando divergência ultrapassar tolerância.
- [x] Exibir relatório legível por dia para depuração rápida.
- [x] Permitir execução local e em CI.

## Implementation Plan
- Criar script `scripts/audit-comparisons-parity.js`.
- Ler dados da API (`/api/performance/comparisons`) e agregar por dia.
- Ler dados do Supabase (`adset_insights`) e agregar por dia.
- Comparar com tolerâncias pequenas de arredondamento e gerar relatório.
- Adicionar comando npm para execução fácil.

## Verification
- [x] Script executa sem falhas no ambiente local.
- [ ] Script detecta divergência real quando os dados são alterados.
- [x] Script passa com os dados reconciliados atuais.
- [x] Workflow do GitHub Actions criado para execução diária.

## Files Modified
- `docs/delivery/28/tasks.md`
- `docs/delivery/28/28-16.md`
- `docs/delivery/28/28-18.md`
- `scripts/audit-comparisons-parity.js`
- `package.json`
- `.github/workflows/audit-comparisons-parity.yml`

## Test Plan
- Rodar script para período `2026-03-15` a `2026-04-13`.
- Validar saída: 0 divergências para baseline reconciliado.
- Simular tolerância baixa e confirmar erro em caso de divergência.
