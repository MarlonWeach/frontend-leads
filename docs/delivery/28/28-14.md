# 28-14 Fortalecer workflow de sync de leads no GitHub Actions

[Voltar para lista de tarefas](./tasks.md)

## Description
Ajustar o workflow de sincronizacao para executar `sync-meta-leads.js` com configuracoes mais estaveis e adicionar validacao/alerta quando o resultado vier zerado.

## Status History
| Data/Hora           | Evento         | De         | Para      | Detalhes | Usuário |
|---------------------|---------------|------------|-----------|----------|---------|
| 2026-04-13 14:20:00 | Status Change | Proposed   | InProgress| Task criada para blindar execucao de leads no GitHub Actions | User/AI Agent |

## Requirements
- Usar `scripts/sync-meta-leads.js` no workflow de leads
- Definir variaveis de estabilidade no `.env.local` do job
- Falhar o job quando o resumo indicar zero leads via formularios
- Preservar logs claros para troubleshooting
- Garantir `.env.local` do CI com `NEXT_PUBLIC_SUPABASE_*` (scripts Node priorizam esses nomes)
- Mitigar falhas transientes `fetch failed` ao salvar campanhas no Supabase (retry)

## Implementation Plan
- Atualizar passo de criacao de `.env.local` no job de leads
- Substituir script legado por `sync-meta-leads.js`
- Capturar log da execucao e validar metrica de leads via formulario
- Encerrar com erro quando houver zero leads

## Verification
- [ ] Workflow executa `sync-meta-leads.js`
- [ ] Variaveis `SYNC_META_LEADS_ONLY_META_FORM_ID` e `SYNC_META_LEADS_FORM_COOLDOWN_MS` aplicadas
- [ ] Pipeline falha com mensagem explicita se `Leads via formularios` for `0`

## Files Modified
- .github/workflows/sync-dados-3x-dia.yml
- scripts/sync-campaigns-once.js
- scripts/sync-meta-leads.js
- scripts/sync-ad-insights-account.js
- docs/delivery/28/tasks.md
- docs/delivery/28/28-14.md

## Test Plan
- Executar workflow manualmente via `workflow_dispatch`
- Conferir no log:
  - `Token para .../leads: META_USER_ACCESS_TOKEN`
  - `Leads via formularios`
  - `leads salvos/atualizados com sucesso`
- Simular falha (opcional) removendo token de leads e validar job em vermelho
