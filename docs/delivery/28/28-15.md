# 28-15 Consolidar documentação de recovery/sync e remover cópias locais do projeto

[Voltar para lista de tarefas](./tasks.md)

## Description
Centralizar em `docs/operations/` os guias de recuperação Supabase, sync Meta e incidentes históricos; versionar scripts operacionais (`sync-all-data.js`, etc.) e `supabase-recovery.sql`; remover pastas aninhadas `frontend-leads/` e `frontend-leads-1/` que duplicam o repositório (nested `.git`, build `.next`).

## Status History
| Data/Hora           | Evento         | De         | Para   | Detalhes |
|---------------------|---------------|------------|--------|----------|
| 2026-04-13 | Status Change | Proposed | Done | Docs movidas, scripts commitados, cópias removidas, `.gitignore` atualizado |

## Requirements
- [x] Avaliar `frontend-leads/` e `frontend-leads-1/` — conclusão: clones antigos, sem conteúdo a mesclar
- [x] Índice em `docs/operations/README.md`
- [x] Scripts de apoio e SQL de recovery no repositório
- [x] Evitar recriação acidental das pastas duplicadas (gitignore)

## Implementation Plan
- Mover MDs soltos de `docs/` para `docs/operations/`
- Adicionar README índice
- `git add` scripts + `supabase-recovery.sql` + `app/favicon.ico/route.ts` (favicon dinâmico)
- Remover diretórios duplicados
- Registrar no backlog de tarefas PBI 28

## Verification
- [x] `docs/operations/README.md` lista documentos e scripts
- [x] Nenhuma pasta `frontend-leads*` no root após limpeza
- [x] Commit único com mensagem clara

## Files Modified
- `docs/operations/*` (novos/movidos)
- `scripts/check-tables-data.js`, `restore-supabase-tables.js`, `sync-all-data.js`, `sync-missing-tables.js`, `validate-sql.js`
- `supabase-recovery.sql`
- `app/favicon.ico/route.ts`
- `.gitignore`

## Test Plan
- Abrir `docs/operations/README.md` e validar links relativos
- Opcional: `node scripts/validate-sql.js` se o script exigir ambiente
