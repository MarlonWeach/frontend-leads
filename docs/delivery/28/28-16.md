# 28-16 Corrigir defasagem e ordenação do heatmap de performance

[Voltar para lista de tarefas](./tasks.md)

## Description
Corrigir o deslocamento de datas (D-1) e a ordenação do heatmap em `/performance`, onde os dados exibidos por dia estão desalinhados em relação à Meta e ao SQL validado no Supabase.

## Status History
| Data/Hora | Evento | De | Para | Detalhes |
|-----------|--------|----|------|----------|
| 2026-04-14 | Status Change | Proposed | Proposed | Task criada para tratar timezone e ordenação do heatmap |
| 2026-04-14 | Status Change | Proposed | InProgress | Implementação iniciada no pipeline de data do heatmap |
| 2026-04-14 | Status Change | InProgress | Done | Correções de timezone/ordenação validadas em produção pelo usuário |

## Requirements
- [x] Confirmar a origem dos dados usados no heatmap (API/Hook/transformação).
- [x] Padronizar cálculo de datas para timezone `America/Sao_Paulo` no pipeline do heatmap.
- [x] Garantir ordenação cronológica correta (sem inversão e sem deslocamento de dia).
- [x] Validar paridade com SQL para período diário (ex.: D-1).
- [x] Evitar regressão nas páginas `/dashboard` e `/performance`.

## Implementation Plan
- Mapear onde o heatmap transforma datas (backend e frontend).
- Remover conversões UTC que deslocem `YYYY-MM-DD` para dia anterior/posterior.
- Ajustar ordenação por data com chave normalizada de dia local.
- Validar com amostras reais comparando UI vs SQL.

## Verification
- [x] Heatmap exibe o dia correto para `ontem` e janela de 7/30 dias.
- [x] Métricas por dia no heatmap batem com query SQL equivalente.
- [x] Sem erros de console relacionados a parsing/conversão de datas.

## Files Modified
- `src/hooks/useHeatmapData.ts`
- `src/components/insights/PerformanceHeatmap.tsx`
- `docs/delivery/28/28-16.md`
- `docs/delivery/28/tasks.md`

## Test Plan
- Selecionar período com data conhecida (ex.: 13/04) e comparar:
  - Valor no heatmap
  - Valor no SQL (`adset_insights`/fonte usada pela tela)
- Repetir validação para 7 dias e 30 dias.
- Validar ordenação visual dos blocos e tooltip por dia.
