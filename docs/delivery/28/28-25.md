# 28-25 Migrar OpenAI para gpt-5-nano

[Voltar para lista de tarefas](./tasks.md)

## Description
Padronizar o uso do modelo OpenAI para `gpt-5-nano` nos fluxos de IA do projeto, reduzindo custo por requisição e alinhando logs/billing com o modelo realmente utilizado.

## Status History
| Data/Hora | Evento | De | Para | Detalhes |
|-----------|--------|----|------|----------|
| 2026-04-15 | Status Change | - | InProgress | Task iniciada para migrar modelo OpenAI para `gpt-5-nano` |

## Requirements
- [x] Configurar `gpt-5-nano` como modelo padrão do OpenAI.
- [x] Remover hardcodes legados de `gpt-4`/`gpt-4o-mini` nas rotas centrais de IA.
- [x] Alinhar logs de uso e estimativa de custo ao novo modelo.
- [ ] Validar comportamento em `/performance` (Performance, Variações, Otimização).

## Implementation Plan
- Atualizar `AI_CONFIG` para usar `gpt-5-nano`.
- Substituir hardcodes de modelo em análise de anomalias, otimização e teste de conexão.
- Ajustar estimativa de custos no logger e endpoint de billing para o novo modelo padrão.
- Validar build local e revisar lints nos arquivos alterados.

## Verification
- [ ] `npm run build` sem erros.
- [ ] `GET /api/ai/test-connection` refletindo novo modelo.
- [ ] Fluxos de IA na página `/performance` retornando com `modelUsed` atualizado.

## Files Modified
- `docs/delivery/28/tasks.md`
- `docs/delivery/28/28-25.md`
- `src/lib/ai/config.ts`
- `src/lib/ai/optimizationEngine.ts`
- `src/lib/ai/anomalyDetection.ts`
- `src/lib/ai/logger.ts`
- `app/api/ai/billing/route.ts`
- `app/api/ai/analyze/route.ts`
- `app/api/ai/optimization/route.ts`
- `app/api/ai/anomalies/route.ts`
- `app/api/ai/test-connection/route.ts`

## Test Plan
- Executar `npm run build`.
- Chamar `GET /api/ai/test-connection`.
- Validar chamadas de IA em `/performance` para garantir respostas e telemetria coerentes.
