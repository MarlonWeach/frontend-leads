# 29-1 Auditoria Supabase: RLS, GRANTs e painel do projeto

## Description
Inventário técnico das políticas RLS e visibilidade das tabelas no projeto Supabase novo; validação manual no dashboard (API, Auth, Network, backups).

## Status History
| Data | Evento | Status |
|------|--------|--------|
| 2026-04-13 | Início PBI 29 | InProgress |

## Requirements
- [ ] Rodar no SQL Editor queries de inventário (`relrowsecurity`, políticas, grants relevantes).
- [ ] Preencher checklist em `security-assessment-2026-04-13.md` §4.
- [ ] Atualizar achados S1–S5 com evidência do projeto real (não só migrations).

## Implementation Plan
1. Copiar queries de inventário do comentário no final deste arquivo.
2. Comparar resultado com migrations em `supabase/migrations/`.
3. Registrar gaps em `security-assessment` (nova versão datada se necessário).

## Verification
- Documento de achados atualizado ou seção "Confirmado em produção".

## Files Modified
- `docs/delivery/29/security-assessment-*.md`

## SQL sugerido (SQL Editor)

```sql
-- Tabelas public sem RLS
SELECT c.relname, c.relrowsecurity
FROM pg_class c
JOIN pg_namespace n ON n.oid = c.relnamespace
WHERE n.nspname = 'public' AND c.relkind = 'r'
ORDER BY c.relrowsecurity, c.relname;

-- Políticas existentes
SELECT schemaname, tablename, policyname, permissive, roles, cmd, qual
FROM pg_policies
WHERE schemaname = 'public'
ORDER BY tablename, policyname;
```

## Test Plan
- N/A (revisão documental + SQL read-only).
