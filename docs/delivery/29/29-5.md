# 29-5 Endurecimento de base de dados (RLS, funções, exec_sql)

## Description
Aplicar migração `20260414140000_security_rls_and_function_hardening.sql`: RLS nas tabelas públicas relevantes, `search_path` fixo nas funções sinalizadas pelo linter, e `exec_sql` restrito a `service_role`. A migração é **idempotente em relação a tabelas em falta** (ex.: legado `wp_*` não criado no projeto atual).

## Status
Done — migração aplicada, app validada e checks críticos do Database Linter sem erros/avisos.

## Requirements
- [x] Executar migração no projeto Supabase (CLI ou SQL Editor).
- [x] Confirmar **Database Linter**: 0 erros `rls_disabled_in_public` nas tabelas existentes; 0 avisos `function_search_path_mutable` nas funções cobertas.
- [x] Confirmar app: dashboard e APIs críticas com `SUPABASE_SERVICE_ROLE_KEY` em produção.
- [x] Scripts que usam `rpc('exec_sql')` apenas com **service role** (não anon).

## Implementation Plan
1. `supabase db push` ou colar SQL no painel.
2. Se alguma tabela core faltar, criar via migrations anteriores antes de reexecutar políticas.
3. Opcional: repetir linter no dashboard (Database → Advisors).

## Verification
- Build local: `npm run build`.
- Smoke: página campanhas / performance / leads.
- Deploy produção realizado com smoke de APIs:
  - `GET /api/performance/comparisons` (mesmo dia) = 200
  - `GET /api/performance` = 200
  - `POST /api/meta/ads` = 200
- Database Linter (Supabase Security Advisor):
  - Errors: 0
  - Warnings: 0
  - Infos: sugestões `rls_enabled_no_policy` (registradas como recomendação residual, sem bloquear conclusão da task)

## Files Modified
- `supabase/migrations/20260414140000_security_rls_and_function_hardening.sql`
- `supabase/migrations/20260414183000_meta_activity_logs_unique_constraint.sql`
- Código servidor já migrado para `supabaseServer` (tasks anteriores).

## Test Plan
- Objetivo: migração aplica sem erro mesmo sem tabelas `wp_*`.
- Critério: SQL completo executa no projeto; linter Supabase sem regressões novas.
