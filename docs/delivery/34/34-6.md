# 34-6 Corrigir build do deploy Vercel em `/login`

## Description
Corrigir falha de build no deploy do Vercel causada por `useSearchParams()` em `app/login/page.tsx` sem boundary de `Suspense`, que interrompe o prerender da rota `/login`.

## Status History
| Date | From | To | Notes |
|------|------|----|-------|
| 2026-04-22 | Proposed | InProgress | Task iniciada para remover erro `missing-suspense-with-csr-bailout` no build |
| 2026-04-22 | InProgress | Done | `Suspense` aplicado em `/login` e `npm run build` executado com sucesso |

## Requirements
- [x] Página `/login` não deve quebrar o prerender por uso de `useSearchParams()`.
- [x] Componente com `useSearchParams()` deve estar dentro de `Suspense`.
- [x] Build local (`npm run build`) deve concluir sem erro para `/login`.

## Implementation Plan
1. Refatorar `app/login/page.tsx` para separar o conteúdo que usa `useSearchParams()` em componente interno.
2. Envolver o componente interno com `Suspense` no export default da página.
3. Adicionar fallback mínimo para estado de carregamento.
4. Executar build local e validar ausência do erro no `/login`.

## Verification
- [x] Executar `npm run build`.
- [x] Confirmar que não há erro `useSearchParams() should be wrapped in a suspense boundary at page "/login"`.

## Files Modified
- `app/login/page.tsx`
- `docs/delivery/34/tasks.md`
- `docs/delivery/34/34-6.md`

## Test Plan
- **Objective:** Garantir build estável para deploy no Vercel.
- **Scope:** Fluxo de build/prerender da rota `/login`.
- **Env/Setup:** Ambiente local com dependências do projeto instaladas.
- **Mock Strategy:** Não aplicável.
- **Scenarios:**
  - Build completo do projeto.
  - Verificação de ausência de erro específico do `/login`.
- **Criteria:** Build finaliza com sucesso sem erro de `Suspense` em `/login`.
