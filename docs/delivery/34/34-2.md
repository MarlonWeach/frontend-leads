# 34-2 Corrigir function search_path mutable

## Description
Eliminar o warning `function_search_path_mutable` da função `public.update_cache_stats_updated_at` definindo `SET search_path` explícito na função de trigger.

## Status History
| Date | From | To | Notes |
|------|------|----|-------|
| 2026-04-22 | Proposed | InProgress | Implementação iniciada via migration idempotente |
| 2026-04-22 | InProgress | Done | Função ajustada com `SET search_path = public` |

## Requirements
- [x] Função `public.update_cache_stats_updated_at` com `SET search_path` explícito.
- [x] Alteração aplicada em migration versionada e idempotente.
- [x] Sem alteração comportamental da trigger (`updated_at`).

## Implementation Plan
1. Criar migration no padrão do projeto.
2. Recriar função com assinatura atual e `SET search_path = public`.
3. Preservar comportamento de atualização de `updated_at`.

## Verification
- [x] Migration criada com ajuste da função.
- [x] SQL preserva o retorno de trigger esperado (`NEW`).

## Files Modified
- `supabase/migrations/20260422190000_34_security_linter_hardening.sql`
- `docs/delivery/34/34-2.md`

## Test Plan
- **Objective:** remover warning de search_path mutável.
- **Scope:** função `update_cache_stats_updated_at`.
- **Env/Setup:** aplicar migration em ambiente Supabase alvo.
- **Mock Strategy:** não aplicável.
- **Scenarios:** executar Security Advisor após migração.
- **Criteria:** alerta `function_search_path_mutable` não deve mais aparecer para a função em escopo.
