# 30-3 Proteger rotas de página com middleware/auth guard

[Voltar para lista de tarefas](./tasks.md)

## Description
Implementar proteção de páginas privadas via middleware para impedir acesso não autenticado e redirecionar para `/login`.

## Status History
| Data/Hora | Evento | De | Para | Detalhes |
|-----------|--------|----|------|----------|
| 2026-04-15 | Status Change | - | Proposed | Task criada para proteção das páginas privadas |
| 2026-04-15 | Status Change | Proposed | InProgress | Implementação de auth guard em middleware iniciada |
| 2026-04-15 | Status Change | InProgress | Done | Guard ativo para páginas privadas com redirecionamento para login |

## Requirements
- [x] Bloquear acesso sem sessão nas páginas privadas.
- [x] Redirecionar para `/login` quando não autenticado.
- [x] Evitar loop de redirecionamento em `/login`.
- [x] Manter compatibilidade com controles já existentes no middleware.

## Implementation Plan
- Definir lista central de rotas privadas.
- Adicionar verificação de cookie de sessão no middleware.
- Redirecionar para `/login` com parâmetro `redirect` da rota original.
- Permitir redirecionamento de `/login` para `/dashboard` quando já houver sessão.

## Verification
- [x] Acesso a `/dashboard` sem cookie de sessão redireciona para `/login`.
- [x] Acesso a `/performance`, `/campaigns`, `/ads`, `/adsets`, `/leads`, `/settings` sem sessão é bloqueado.
- [x] Usuário com cookie acessa páginas privadas normalmente.
- [x] `npm run build` executado com sucesso.

## Files Modified
- `docs/delivery/30/tasks.md`
- `docs/delivery/30/30-3.md`
- `src/middleware.ts`

## Test Plan
- Testar manualmente:
  1. Limpar cookies de sessão.
  2. Acessar rotas privadas e confirmar redirecionamento para `/login`.
  3. Fazer login e confirmar acesso normal às páginas privadas.
  4. Acessar `/login` com sessão ativa e confirmar redirecionamento para `/dashboard`.
