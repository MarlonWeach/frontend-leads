# 32-7 Implementar motor de recomendações assistidas + auditoria

[Voltar para lista de tarefas](./tasks.md)

## Description
Motor v1 (stub) com persistência, APIs REST conforme [32-3](./32-3.md), UI mínima em `/dashboard/recomendacoes` conforme [32-4](./32-4.md), e refatoração compartilhada do contexto de metas.

## Status History
| Data/Hora | Evento | De | Para | Detalhes |
|-----------|--------|----|------|----------|
| 2026-04-17 | Status Change | - | Done | APIs optimization, generate stub, página, middleware, `buildOptimizationContextItems` |

## Requirements
- [x] `GET/POST` em `/api/optimization/recommendations` (+ generate, history, `:id`, `:id/decision`).
- [x] Autenticação via cookie + `getAuthenticatedUserId`; piloto `isPilotMetaAccountAllowed`.
- [x] Expiração lazy (`expireStaleRecommendations`).
- [x] Motor stub: CPL vs alvo + janela 7d + meta (`generateRecommendationsStub`).
- [x] UI lista + modais apply/discard/defer + botão gerar.
- [x] Refatorar `optimization-context` para `buildOptimizationContextItems`.

## Files Modified

- `package.json` (script `build:clean`)
- `app/api/goals/optimization-context/route.ts`
- `app/api/optimization/recommendations/route.ts`
- `app/api/optimization/recommendations/generate/route.ts`
- `app/api/optimization/recommendations/history/route.ts`
- `app/api/optimization/recommendations/[id]/route.ts`
- `app/api/optimization/recommendations/[id]/decision/route.ts`
- `app/dashboard/recomendacoes/page.tsx`
- `app/recomendacoes/page.tsx`
- `src/middleware.ts`
- `src/components/MainLayout.tsx`
- `src/components/recommendations/RecommendationsPanel.tsx`
- `src/lib/auth/serverSession.ts`
- `src/lib/goals/optimizationContext.ts`
- `src/lib/optimization/adsetInsightsWindow7d.ts` (já existia)
- `src/lib/optimization/expireRecommendations.ts`
- `src/lib/optimization/generateRecommendationsStub.ts`
- `src/lib/optimization/metaAccountAccess.ts`
- `src/lib/optimization/recommendationRowMapper.ts`
- `src/types/optimizationRecommendations.ts`

## Aplicar a migration no Supabase (staging / produção)

O arquivo é: `supabase/migrations/20260417120000_optimization_recommendations_tables.sql`.

**Opção A — SQL Editor (recomendado se não usar CLI linkada)**  
1. Abrir o projeto no [Supabase Dashboard](https://supabase.com/dashboard) → **SQL Editor**.  
2. Colar o conteúdo completo do arquivo acima.  
3. Executar (**Run**). Confirmar que `optimization_recommendations` e `optimization_recommendation_decisions` aparecem em **Table Editor**.

**Opção B — CLI** (requer Docker local ou projeto linkado)  
Na raiz do repositório, com `supabase link` já configurado: `supabase db push`.

**Após aplicar:** em dev, `npm run dev` e testar **Gerar sugestões** em `/dashboard/recomendacoes`.

## Verification

- [x] `npx tsc --noEmit`
- [x] `npm test -- test/integration/api/goals/optimization-context.test.ts`
- [x] `npm run build` ou `npm run build:clean` (limpa `.next` se aparecer `PageNotFoundError` fantasma em `/adsets` / rotas API)

## Test Plan

Objetivo: integração manual após `supabase db push` (migration `20260417120000_optimization_recommendations_tables.sql`).

Cenários:

1. Login → `/recomendacoes` → Gerar sugestões → listar ativas.
2. Aplicar / descartar / adiar e conferir linhas em `optimization_recommendation_decisions`.
3. Recomendação expirada → POST decision retorna 409 `RECOMMENDATION_EXPIRED`.

## Implementation Plan (follow-up)

- Testes de integração Jest para rotas `/api/optimization/*` com mocks.
- Script offline [32-5](./32-5.md) consumindo o mesmo stub.
- Integração Meta para “aplicar” budget (fora do escopo desta task).
