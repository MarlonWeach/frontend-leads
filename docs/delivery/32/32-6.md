# 32-6 Implementar coleta/agregação das dimensões do MVP

[Voltar para lista de tarefas](./tasks.md)

## Description
Entregar a **base técnica mínima** para recomendações assistidas: persistência conforme [32-3](./32-3.md), agregação da janela de 7 dias por adset (32-1) e insumos para o motor na **32-7**.

## Status History
| Data/Hora | Evento | De | Para | Detalhes |
|-----------|--------|----|------|----------|
| 2026-04-17 | Status Change | - | InProgress | Migration `optimization_recommendations` + `optimization_recommendation_decisions`; RLS sem política permissiva |
| 2026-04-17 | Status Change | InProgress | Done | Agregação 7d (`aggregateAdsetInsightsWindow7d`) + testes; sim offline fica para 32-7 |

## Requirements
- [x] Tabelas Supabase + índices + CHECKs alinhados ao contrato 32-3.
- [x] RLS habilitado; sem grants amplos a `anon`/`authenticated` (acesso via `service_role` nas APIs).
- [x] Módulo TypeScript de agregação **7d** (`src/lib/optimization/adsetInsightsWindow7d.ts`) para reuso pelo motor.
- [ ] Script/job de simulação offline ([32-5](./32-5.md)) — **32-7** ou script dedicado em `scripts/`.

## Implementation Plan

1. ~~Migration das tabelas de recomendação e decisões.~~
2. ~~`aggregateAdsetInsightsWindow7d` + teste unitário.~~
3. Wire na geração diária e APIs na **32-7**.

## Verification

- [x] Migration consistente com função `public.update_updated_at_column` (migração `20260414140000` aplicada antes).
- [x] Agregação 7d com teste unitário (`npm test -- test/unit/lib/optimization/adsetInsightsWindow7d.test.ts`).

## Files Modified

- `supabase/migrations/20260417120000_optimization_recommendations_tables.sql`
- `src/lib/optimization/adsetInsightsWindow7d.ts`
- `test/unit/lib/optimization/adsetInsightsWindow7d.test.ts`
- `docs/delivery/32/32-6.md`
- `docs/delivery/32/tasks.md`

## Test Plan

Objetivo: schema pronto para integração na 32-7.

Critérios: `INSERT` + `INSERT` decisão via service role; `anon` sem `SELECT` nas novas tabelas (validar em projeto com RLS checks).
